name: Build APK

on:
  push:
    paths:
      - 'pubspec.yaml'
      - 'lib/**'
      - 'android/**'
      - '.github/**'
    branches: [main]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Backup source files
        run: |
          echo "Backing up lib/ and pubspec.yaml"
          tar czf source_backup.tar.gz lib pubspec.yaml
          ls -la

      - name: Regenerate Android project
        run: |
          rm -rf android
          flutter create --platforms=android --no-pub .

      - name: Restore source files
        run: |
          echo "Restoring lib/ and pubspec.yaml"
          tar xzf source_backup.tar.gz
          ls lib/

      - name: Flutter pub get
        run: flutter pub get

      - name: Build APK (arm64)
        run: flutter build apk --release --split-per-abi --target-platform android-arm64

      - name: Show output
        run: |
          find build/app/outputs/flutter-apk/ -name "*.apk" | xargs ls -la

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: kasku-app-arm64
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
